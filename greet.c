#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{if(argc == 2)
{

    printf("hello, %s\n", argv[1]);
}
else
{
    printf("hello, world\n");
}
    // string name = get_string("What is your name? ");
    // printf("hello, %s %s\n", argv[1], argv[2]);
}